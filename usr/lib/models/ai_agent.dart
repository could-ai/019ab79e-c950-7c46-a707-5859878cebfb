class AiAgent {
  final String id;
  final String name;
  final String description;
  final String icon; // Using emoji for simplicity in this demo

  AiAgent({
    required this.id,
    required this.name,
    required this.description,
    required this.icon,
  });
}
